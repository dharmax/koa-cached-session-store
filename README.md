# koa-cached-session-store
A decorator for session store that calls the underlying [database-driven] store with a simple caching mechanism, and thus makes the session both persistent and very efficient
